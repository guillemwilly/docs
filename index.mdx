---
title: Welcome to Agno
description: Build, run, manage agents at scale.
---

Agno turns agents into a production service you can run anywhere.

- **Build** your agent in any framework.
- **Run** it as a production service using AgentOS.
- **Interact** via the AgentOS UI, Slack, Discord, or your own client.

<CodeGroup>

```python Agno SDK
from agno.agent import Agent
from agno.db.sqlite import SqliteDb
from agno.os import AgentOS
from agno.tools.hackernews import HackerNewsTools

agent = Agent(
    name="Agno Agent",
    model="openai:gpt-5.4",
    tools=[HackerNewsTools()],
)

agent_os = AgentOS(
    agents=[agent],
    tracing=True,
    db=SqliteDb(db_file="tmp/agentos.db"),
)
app = agent_os.get_app()

if __name__ == "__main__":
    agent_os.serve(app="agno_agent:app", reload=True)
```

```python Claude Agent SDK
from agno.agents.claude import ClaudeAgent
from agno.db.sqlite import SqliteDb
from agno.os import AgentOS

agent = ClaudeAgent(
    name="Claude Agent",
    model="claude-opus-4-7",
    allowed_tools=["Read", "Bash"],
    permission_mode="acceptEdits",
)

agent_os = AgentOS(
    agents=[agent],
    tracing=True,
    db=SqliteDb(db_file="tmp/agentos.db"),
)
app = agent_os.get_app()

if __name__ == "__main__":
    agent_os.serve(app="claude_agent:app", reload=True)
```

```python DSPy
import dspy
from agno.agents.dspy import DSPyAgent
from agno.db.sqlite import SqliteDb
from agno.os import AgentOS

dspy.configure(lm=dspy.LM("openai/gpt-5.4"))

agent = DSPyAgent(
    name="DSPy Assistant",
    program=dspy.ChainOfThought("question -> answer"),
)

agent_os = AgentOS(
    agents=[agent],
    tracing=True,
    db=SqliteDb(db_file="tmp/agentos.db"),
)
app = agent_os.get_app()

if __name__ == "__main__":
    agent_os.serve(app="dspy_agent:app", reload=True)
```

```python LangGraph
from agno.agents.langgraph import LangGraphAgent
from agno.db.sqlite import SqliteDb
from agno.os import AgentOS
from langchain_openai import ChatOpenAI
from langgraph.graph import MessagesState, StateGraph

def chatbot(state: MessagesState):
    return {"messages": [ChatOpenAI(model="gpt-5.4").invoke(state["messages"])]}

graph = StateGraph(MessagesState)
graph.add_node("chatbot", chatbot)
graph.set_entry_point("chatbot")
compiled = graph.compile()

agent = LangGraphAgent(
    name="LangGraph Chatbot",
    graph=compiled,
)

agent_os = AgentOS(
    agents=[agent],
    tracing=True,
    db=SqliteDb(db_file="tmp/agentos.db"),
)
app = agent_os.get_app()

if __name__ == "__main__":
    agent_os.serve(app="langgraph_agent:app", reload=True)
```

</CodeGroup>

## Why Agno

<div className="grid grid-cols-1 gap-10 md:grid-cols-2 md:gap-x-12 md:gap-y-10 mt-6">
  <div className="border-t border-zinc-200 dark:border-zinc-800 pt-5">
    <h3 className="text-base font-semibold mb-2 mt-0">Fastest path from prototype to production</h3>
    <p className="text-sm leading-relaxed text-zinc-600 dark:text-zinc-400 m-0">
      One SDK, one runtime, one deploy. No glue code between framework, API layer, database, and infra.
    </p>
  </div>

  <div className="border-t border-zinc-200 dark:border-zinc-800 pt-5">
    <h3 className="text-base font-semibold mb-2 mt-0">Production-ready out of the box</h3>
    <p className="text-sm leading-relaxed text-zinc-600 dark:text-zinc-400 m-0">
      Storage, tracing, full audit logs, runtime enforcement. The boring parts of production, already done.
    </p>
  </div>

  <div className="border-t border-zinc-200 dark:border-zinc-800 pt-5">
    <h3 className="text-base font-semibold mb-2 mt-0">Built for streaming and long-running agents</h3>
    <p className="text-sm leading-relaxed text-zinc-600 dark:text-zinc-400 m-0">
      Pause, resume, stream in real time. Human-in-the-loop, background jobs, days-long workflows. The runtime treats agentic patterns as first-class.
    </p>
  </div>

  <div className="border-t border-zinc-200 dark:border-zinc-800 pt-5">
    <h3 className="text-base font-semibold mb-2 mt-0">Secure by default</h3>
    <p className="text-sm leading-relaxed text-zinc-600 dark:text-zinc-400 m-0">
      JWT verification, RBAC, per-user-per-session isolation, multi-tenancy. First-class primitives in the runtime, not a homework assignment.
    </p>
  </div>

  <div className="border-t border-zinc-200 dark:border-zinc-800 pt-5">
    <h3 className="text-base font-semibold mb-2 mt-0">You own everything</h3>
    <p className="text-sm leading-relaxed text-zinc-600 dark:text-zinc-400 m-0">
      Agno runs on your infrastructure. Own the data, pay your cloud directly.
    </p>
  </div>

  <div className="border-t border-zinc-200 dark:border-zinc-800 pt-5">
    <h3 className="text-base font-semibold mb-2 mt-0">No lock-in, completely <i>agno</i>stic</h3>
    <p className="text-sm leading-relaxed text-zinc-600 dark:text-zinc-400 m-0">
      Agno SDK, Claude Agent SDK, DSPy, LangGraph. OpenAI, Anthropic, Gemini. Postgres, SQLite, any vector db. Swap any piece, any time.
    </p>
  </div>
</div>